package im.server.logic.distributed

/*
class RabbitMqSender(val template: RabbitTemplate, val exchange: String) : MQSender {
    private val logger = LoggerFactory.getLogger(this.javaClass)


    */
/*override fun send(server_id: String, msg: Msg) {
        template.convertAndSend(exchange, server_id, msg)
    }*//*


    override fun send(server_id: String, action: Any) {
        template.convertAndSend(exchange, server_id, action)
    }
}*/
